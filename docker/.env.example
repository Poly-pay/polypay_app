# Postgres
POSTGRES_PASSWORD=polypay_password

# Backend
NODE_ENV=production
BACKEND_PORT=4000
CORS_ORIGIN="http://localhost:3000"
API_PREFIX="api"
LOG_LEVEL="info"

# JWT (REQUIRED for auth)
JWT_SECRET="your-jwt-secret-key-here-change-in-production"
JWT_REFRESH_SECRET="your-jwt-refresh-secret-key-here-change-in-production"

# Relayer zkVerify API Key
RELAYER_ZKVERIFY_API_KEY="your-relayer-zkverify-api-key-here"

# Relayer Wallet Private Key (For deploy and execute smart contract)
RELAYER_WALLET_KEY="your-relayer-wallet-private-key-here"

# Database (for docker - connects to postgres service)
DATABASE_URL="postgresql://polypay_user:${POSTGRES_PASSWORD}@postgres:5432/polypay_multisig_db"

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000
